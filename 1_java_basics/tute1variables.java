public class tute1variables {
    public static void main(String[] args) {
        int a = 25;                  //there are two types of data type (a)primitive(b) non primitive
                                             // byte,short,char,boolian,int,long,float,double are primitive data type
                                           //String,Array, Class, Object,Interface are nonpremitive datatype
 int b = 27;
 int mul = (a * b);
 int sub = (a - b);
System.out.print((mul) / (sub));
    }
}
